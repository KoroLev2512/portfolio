import stubPortraitSrc from '@/../public/stub-portrait.svg'

/** Neutral placeholder portrait when CMS content is disabled (not `avatar.png`, which may be a real headshot). */
export { stubPortraitSrc }
